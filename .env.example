# PostgreSQL — local install only (no Docker database). PG_HOST=localhost for `python run.py`.
PG_USER=secretscan
PG_PASSWORD=bmcAdm1n@123
PG_DATABASE=secrets_db
PG_HOST=localhost
PG_PORT=5432
# psql helper: export DATABASE_URL="postgresql://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DATABASE}"

# Application
PORT=5002
JWT_SECRET=change-me-in-production-use-a-long-random-string

# Jenkins (optional — mock scan runs if unset)
JENKINS_URL=
JENKINS_JOB_NAME=
JENKINS_USER=
JENKINS_API_TOKEN=
JENKINS_PARAM_REPO=REPO_URL
JENKINS_PARAM_BRANCH=BRANCHES
